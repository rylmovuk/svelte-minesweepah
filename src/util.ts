// export function range(n: number): number[] {
//   return Array.from({ length: n }, (_v, i) => i);
// }

import { sumBy } from "lodash-es";

// export function sample<T>(array: T[], n: number): T[] {
//   let arrCopy = [...array];
//   let sample = [];
//   for (let i = 0; i < n; ++i) {
//     const randIndex = Math.floor(Math.random() * arrCopy.length);
//     let [elem] = arrCopy.splice(randIndex, 1);
//     sample.push(elem);
//   }
//   return sample;
// }

// export function count<T>(iter: Iterable<T>, predicate: (val: T) => boolean): number {
//   return sum(map(iter, val => predicate(val) as unknown as 0 | 1))
// }
